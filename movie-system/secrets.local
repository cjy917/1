TMDB_API_KEY=f0bdfd8195d0d1d5a14c17259212336a
# EXTERNAL_HTTP_PROXY 留空 = 让系统路由自动处理：
#   - Windows：http_client.py 会自动读注册表的 Clash 系统代理
#   - Mac Shadowrocket TUN/VPN：空配置直接走虚拟网卡拦截
# 如果需要强制指定某代理，才取消下一行注释
# EXTERNAL_HTTP_PROXY=http://127.0.0.1:7890

# ==============================================
# AI智能语音小助手（硅基流动 SiliconFlow 免费API）
# ==============================================
# 注册获取免费 API Key：https://cloud.siliconflow.cn/
# 注册后点击「API密钥」→ 「创建新密钥」，复制粘贴到下方：
AI_ASSISTANT_API_KEY=sk-urppbzqcmoelkdtovsiouwocmuozuvqxektwiytpxrvauwun
#
# （可选配置，默认值如下，如需自定义取消注释并修改）
# AI_ASSISTANT_ENABLED=true
# AI_ASSISTANT_API_BASE=https://api.siliconflow.cn/v1
# AI_ASSISTANT_MODEL=Qwen/Qwen2.5-7B-Instruct
# AI_ASSISTANT_TIMEOUT=30
# AI_ASSISTANT_MAX_TOKENS=1024

# ==============================================
# AI 助手联网搜索（可选，默认关闭）
# ==============================================
# 当用户问「最近新上映」「实时票房」「本周热门」时，系统会自动尝试联网获取最新数据
# 默认 AI_WEB_SEARCH_ENABLED=false，不配置 API Key 也完全可以正常使用（查询系统内部数据库为主）
# 如需开启联网搜索功能：
# 1. 注册 Tavily 免费 API Key：https://tavily.com/ （每月 1000 次免费查询）
# 2. 取消下方两行注释并填入 Key：
AI_WEB_SEARCH_ENABLED=true
AI_WEB_SEARCH_API_KEY=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
#
# （可选配置，默认值如下，如需自定义取消注释并修改）
# AI_WEB_SEARCH_PROVIDER=tavily
# AI_WEB_SEARCH_MAX_RESULTS=3

# ==============================================
# 网络代理配置（macOS Shadowrocket/Clash/系统代理 自动支持）
# ==============================================
# ✨ 好消息：在 macOS 下你不用手动配置固定端口！
# 只要你打开 Shadowrocket 并勾选了「设置 → 系统代理」，
# 后端会自动执行 `scutil --proxy` 读取系统设置里的
# HTTP/HTTPS/SOCKS 代理地址和动态端口，无需手动修改下方配置。
#
# 如果自动检测失败（日志里显示「代理=禁用」但 Shadowrocket 明明开着），
# 才需要手动配置固定端口：
#
# 方法 A (推荐，更稳定)：
#   在 Shadowrocket → 首页 → 点击节点旁的 ⓘ → 开启「HTTP 代理」
#   → 自定义固定 HTTP 端口（如 8888），然后取消下方注释：
# HTTP_PROXY=http://127.0.0.1:8888
# HTTPS_PROXY=http://127.0.0.1:8888
#
# 方法 B (如果 Shadowrocket 只开了 SOCKS5 代理)：
#   → 取消下面注释，端口替换成 Shadowrocket 实际的 SOCKS 端口（通常是动态的）
# SOCKS_PROXY=socks5://127.0.0.1:端口号
#
# NO_PROXY（不要走代理的域名/内网地址）一般默认即可，不用改：
# NO_PROXY=127.0.0.1,localhost,192.168.*,10.*
#
# 验证方法：重启 Flask 后端后发送任意一条消息给 AI 小助手，
# 查看控制台日志中这两行：
#   [SiliconFlow] POST ... 开始请求 代理=启用(http,https)  ← ✅ 说明成功走了代理
#   [SiliconFlow] 当前代理配置：OS 检测：HTTP=http://127.0.0.1:xxxx HTTPS=... SOCKS=socks5://...


